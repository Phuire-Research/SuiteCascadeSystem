# Gitm Resolver — Aspect Transplant Manifest (Maintainer.md)

## Preamble — the Boundary Law

Designation, Home SCP, and Dependencies are **identity-bearing** at any future update
seam. The part-renewal doctrine that guards the SCP self-update circuit (C293: identity-bearing
files take disposition WRITE — full theirs; NEVER patch) applies one level up to Suite-8 identity:
a partial update MUST NOT revert this Suite 8's Designation, re-home it, or silently drop a
declared Dependency. The Sovereignty Boundary below names what is portable and what is fixed —
the boundary makes transfer meaningful; this manifest makes the boundary crossable.

*Gitm Resolver IS the C293 part-renewal doctrine's operating agent — it applies the priority
rules this Preamble invokes.*

## Sovereignty Boundary

- **Home SCP**: bridge (operating set — NOT migrated to the template)
- **Installed-in**: bridge root only. Gitm Resolver runs the update-merge at the bridge; it
  operates ON installed SCPs but does not ride the install clone itself.
- **Fixed**: Designation (`Gitm Resolver`).

## Skills Registry

| Skill | File | Currency Gate (one-liner) | Last Executed |
|-------|------|---------------------------|---------------|
| S1 Diff-Ingest | `Skill.md` (§S1) | Consumes `scp-update-diff.<name>.json` (D-U2 3-way) | UNKNOWN |
| S2 Auto-Path | `Skill.md` (§S2) | Auto-safe path resolution still current | UNKNOWN |
| S3 Zone-Union | `Skill.md` (§S3) | Unions REGISTERED_MUXONOMICS + BASE_CONCEPTS_CREATORS collision zones | UNKNOWN |
| S4 Conference-Surface | `Skill.md` (§S4) | Surfaces only true `collisionZone===false` overlaps via Staging Tool | UNKNOWN |
| S5 Resolution-Emit | `Skill.md` (§S5) | Emits `scp-update-resolved.<name>.json` (totality invariant) | UNKNOWN |

*Direct config — S1-S5 are documented within `Skill.md`; no `Skills/` subdir.*

## Muxonomy Registration

- **Designation**: Gitm Resolver
- **Configuration**: Direct
- **Domain**: SCP Update Merge Resolution — priority doctrine (user expansions WIN · template
  functionality ATTENDS · overlaps CONFER), zone-union, resolved decision-set emit for the apply
  stage (D-U4/D-U5).
- **Dependencies**: SCS Bridge (the update circuit that feeds the diff + applies the resolved
  set); `git cat-file` (conflict-marker read from resultTree); the Staging Tool (Conference
  surface). Cross-Suite-8: SCS Bridge.

## Cascade Position Marker

- **Gate**: UNKNOWN
- **PENDING**: none evident
