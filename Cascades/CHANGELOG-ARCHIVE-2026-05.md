# SCS Changelog Archive — 2026-05

Rotated from `Cascades/CHANGELOG.md` on 2026-05-07 (Diamond B-4 rotation trigger, file exceeded 150-line cap).

---

### 2026-05-07 (Diamond R)

**01DISCOVER Prefix Replacement + ANSI-Aware Line-Width Discipline — Diamond R** — Two muxified fixes addressing Diamond Q smoke observations. (1) *Synthesized-Aware ULID Column Substitution* (SB-S37) — `formatSessionRow` + `renderMenuLegacy` substitute `claudeSessionId.slice(0,10)` for synthesized entries (where `id.slice(0,10)` produced the repetitive literal `01DISCOVER` across all discovered rows); spawned rows continue to render meaningful ULID prefix. Information-density fix: synthesized rows now show their real Claude UUID prefix. (2) *ANSI-Aware Line-Width Clip-And-Pad Discipline* (SB-S38) — new `clipAndPadToWidth` helper (with `stripAnsiCodes` + `visibleLength`) wraps every emitted line in `renderMenu` (header, status, HEAD, body row, TAIL, footer) AND `renderMenuLegacy`. ANSI_REGEX strips escape sequences for visible-character counting; clip preserves ANSI codes within clipped portion + appends trailing reset codes defensively (selection highlight survives clip). `cwdMaxWidth` formula tightened: `Math.max(20, termWidth-89)` → `Math.max(15, termWidth-92)`. Explicit `padEnd(cwdMaxWidth)` on cwd column makes row width deterministic. **Failing mode CLOSED**: rows overflowing termWidth by 2-4 chars (when relativeTime ≥ 7 chars like "11h ago" or "just now") wrapped onto next visual line; wrapped chars became prefix on subsequent emitted line; phantom Close Bridge + duplicate footer hint accumulated as page rendered longer than allocated bottomRows. SB-S37 NEW + SB-S38 NEW · 37 skills active · 438/438 tests · build 85 KB · `0.19.0 → 0.20.0` · Suite 8 docs v5.8 → v5.9. CD-5 PASS (18th consecutive Diamond clean, C through R). CD-19 candidate *Render-Time Termwidth Invariant Diameter* (every TUI render line exactly termWidth chars after ANSI-aware visible counting; phantom-row class of bug closes structurally; promotable post-user smoke). Generalizes Diamond P Fix P-3 (body slot pad-to-clear) to all-line-emissions. Branch: `SCS-Bridge`. State: TESTING (push HELD pending user-Lambda smoke).

### 2026-05-07 (Diamond Q)

**Session Display Name Rename Feature + animatedTui remove-selected gap CLOSED — Diamond Q** — User-facing organizational labeling for Bridge sessions. `displayName?: string` added to `RegistryEntry` + mirrored in `SessionMeta` (SB-S34 NEW): optional field, backward compat preserved — sessions without displayName render identically to v5.7. `'r'` keypress activates rename-mode modal (SB-S35 NEW): `renameMode?: { ulid, buffer }` on MenuState; printable chars append to buffer; Backspace pops; Enter confirms via `setSessionDisplayName(ulid, buffer.trim() || undefined)` (empty buffer clears displayName); Esc cancels; footer switches to `Rename: <buffer>_  · Enter confirm · Esc cancel` then reverts with `r rename` hint added to standard footer. `formatSessionRow` + `renderMenuLegacy` check `entry.displayName` before emitting uuid-short column; when set, displayName renders in place (32-char truncation with ellipsis) (SB-S36 NEW). Column alignment preserved; Layer-1 termHeight invariant unaffected. **Issue 3 CLOSED (pre-existing gap)**: `animatedTui.ts` keypress dispatch was not handling `remove-selected` action — `'x'` key (SB-S28, Diamond N) was silently no-op in animated TUI; Diamond Q audit surfaced and fixed. SB-S34 NEW + SB-S35 NEW + SB-S36 NEW · 35 skills active · 429/429 tests · build 83 KB · `0.18.0 → 0.19.0` · Suite 8 docs v5.7 → v5.8. CD-5 PASS (17th consecutive Diamond clean, C through Q). **CD-11 through CD-17 CORONATED** (D-Queue cleared — "Working as Intended" pre-Diamond-Q confirmed Diamonds K+K-Fix+L+M+N+O+P). CD-18 candidate *User-Sourced Identification Diameter* (user-assigned names compose with registry-truth without violating Pattern 4; promotable post-Diamond-Q user smoke). Branch: `SCS-Bridge`. State: TESTING (push HELD pending user-Lambda smoke).

### 2026-05-07 (Diamond P)

**Threshold Calibration + Discovered-Session Scaffolding + Pad-To-Clear Render Discipline — Diamond P** — Three muxified fixes addressing residual UX failures from the 12-stage user-Lambda smoke after Diamond O. (1) *Empirical Single-Call Threshold Calibration* (SB-S31) — `BLANK_SIZE_THRESHOLD_BYTES` lowered from 15KB to 5KB; distribution evidence from 16 JSONL files shows 9-10KB cluster (single user prompt + assistant response = real session) was incorrectly classified as blank under the 15KB threshold; 5KB cleanly separates 2-3KB cluster (claude-died-early) from 9KB+ cluster (real exchanges); constant update propagates automatically to all consumers (`hasPersistedSession` + `discoverPersistedSessions`). (2) *Discovered-Session First-Class Scaffolding* (SB-S32) — new `scaffoldDiscoveredSession(ulid, cwd, claudeSessionId, mtimeMs)` helper creates full session directory for auto-discovered entries (synthesized `meta.json` + `spawn-settings.json` with hook injection + empty `archive/`/`body/`/`heads/`/`tails/` dirs); called in `animatedTui.ts` auto-discovery pass after `addSession`; idempotent (skips if meta.json present); Diamond O Fix O-1 `synthesizedAt` gate in `launchInformative` REVERTED (discovered sessions now look identical to spawned sessions from resume perspective — both have `meta.json`, both receive hook on resume, status transitions `launched → alive`). (3) *Body Slot Pad-To-Clear Render Discipline* (SB-S33) — `renderMenu` body section now emits exactly `visibleBodySlots` lines per frame; vacated slots (session count shrank) emit explicit clear-pad row instead of leaving prior frame content; closes phantom-row duplicate UX (user observed unselectable duplicate after closing spawned session). SB-S31 NEW + SB-S32 NEW + SB-S33 NEW · 32 skills active · 403/403 tests · build 78.49 KB · `0.17.0 → 0.18.0` · Suite 8 docs v5.6 → v5.7. CD-5 PASS (16th consecutive Diamond clean, C through P). CD-17 candidate *Empirical-Layer-4 Threshold Validation Diameter* (5KB empirical threshold anchored to observed distribution). Branch: `SCS-Bridge`. State: TESTING (push HELD pending user-Lambda smoke).

### 2026-05-07 (Diamond O)

**Synthesized Meta-Skip + Project-Local Bridge Root Relocation — Diamond O** — Two residual fixes from Diamond N completing the per-project scoping circuit. (1) *Bug O-1 Synthesized-Aware Conditional Meta Read* (SB-S29) — `manager.ts:launchInformative` now gates `loadSessionMeta` behind `entry.synthesizedAt !== undefined` check; auto-discovered `01DISCOVERED-*` sessions have no `sessions/<ulid>/` directory, so `loadSessionMeta` was throwing ENOENT unconditionally (Diamond N Fix N-B was incomplete — it re-ordered claudeSessionId resolution but left the read unconditional). Fix: `if synthesizedAt → use entry.cwd directly, skip loadSessionMeta, skip writeSessionMeta`. Non-synthesized path unchanged. (2) *Bug O-2 Project-Local Bridge State Substrate* (SB-S30) — `paths.ts:bridgeRoot()` now returns `join(process.cwd(), 'Cascades', 'Bridge')` replacing `join(homedir(), '.scs-bridge')`. All consumers updated (`debugLog.ts`, `bridgeStateFeed.ts`). `cli.ts` help text updated. `.gitignore` updated with `Cascades/Bridge/`. Existing `~/.scs-bridge/` is orphaned (no automated migration; auto-discovery via SB-S23 restores session visibility from `~/.claude/projects/`). Bridge state now co-located with Cascades planning artifacts. SB-S29 NEW + SB-S30 NEW · 29 skills active · 397/397 tests · build 78.04 KB · `0.16.0 → 0.17.0` · Suite 8 docs v5.5 → v5.6. CD-5 PASS (15th consecutive Diamond clean, C through O). CD-16 candidate *Project-Local Bridge Capsule Diameter* (per-project scoping = fifth invalidation channel). Branch: `SCS-Bridge`. State: TESTING (push HELD pending user-Lambda smoke).

### 2026-05-07 (Diamond N)

**Auto-Discovery Survival + Resume-Without-Meta + Hook Debug Propagation + Orphan Detection + User Forced Eviction — Diamond N** — Five muxified surface fixes resolving the 12-stage user-Lambda smoke trace from Diamond M: invalidated sessions are now reliably removed from the menu via four independent invalidation channels (CD-14 Trinity → Quartet). (1) *Synthesized-Entry Removal-Path Exemption* (Fix N-A) — both `liveness.ts:probeLivenessTick` AND `animatedTui.ts` blank-filter now route entries with `synthesizedAt` set to aliveIds (no stale-pending sweep, no blank-removal). Auto-discovered sessions persist indefinitely as offline rows until manually removed. (2) *Registry-First Resume Identity Lookup* (Fix N-B) — `manager.ts:launchInformative` consults `listSessions()` BEFORE `loadSessionMeta()` for `claudeSessionId`; meta.json read preserved for `cwd`/`terminalCommand`/`launchedAt` fields. Auto-discovered ULIDs (no meta.json) now resume successfully. (3) *Spawn-Settings Env-Prefix Debug Injection* (Fix N-C) — `spawnSettings.ts:buildSpawnSettings` prepends `SCS_BRIDGE_DEBUG=1 ` to both `commandStart` and `commandEnd` when `isDebugEnabled()` returns true. Hook subprocesses now receive debug flag through Claude Code's env-sanitization barrier; `hook.fire` events appear in `~/.scs-bridge/debug.log`. (4) *Mtime-Advance Orphan Detection* (Fix N-D2) — new `getJsonlMtime(cwd, claudeSessionId)` export in `sessionPersistence.ts` (statSync.mtimeMs metadata read; Pattern 4 Modulation FOURTH channel after existsSync + statSync.size + readdirSync); animatedTui liveness tick maintains module-level `mtimeTracker: Map<string, {mtimeMs, firstSeenMs}>`; if claudePid is alive AND JSONL mtime frozen for `ORPHAN_DETECTION_MS = 90_000` → `markSessionOffline`. Tracker cleaned on every state-change path (orphan/offline/stale/blank). **Note**: ORPHAN_DETECTION_MS = 90s — idle-but-alive sessions exceeding 90s without JSONL writes will transition to offline; can be re-resumed via Enter or removed via 'x'. (5) *User-Driven Forced Eviction Escape Valve* (Fix N-D3) — `menu.ts` adds `'x'` keypress dispatching new `remove-selected` KeyAction; `removeSession(ulid)` is idempotent and JSONL-safe (registry-only mutation); SYNTHETIC_NEW/CLOSE sentinels protected. Footer hint updated. 391/391 tests (367 → 391, +24 across liveness/sessionPersistence/spawnSettings/menu/animatedTui/manager.test.ts NEW) · build 77.73 KB → 79.7 KB packed · `0.15.0 → 0.16.0` · pack invariant 5 files preserved. Wave 14 v2 grep ZERO. Pattern 4 Modulation preserved. CD-15 candidate *Multi-Channel Invalidation Diameter* (hook + PID-death + blank-filter + mtime-freeze quartet) promotable to coronation post user-Lambda smoke. Branch: `SCS-Bridge`. State: TESTING (push HELD pending user-Lambda 12-stage smoke).

### 2026-05-07 (Diamond M)

**Phantom Eviction + Mutex + Auto-Discovery — Diamond M** — Three registry-truth improvements muxified into one Length 1-7 cycle: (1) *Post-SessionEnd Persistence Eviction* — `sessionEndHook.ts` now consults `hasPersistedSession(cwd, claudeSessionId)` at hook-exit time; if NOT persisted → `removeSession` immediately (phantom row disappears on close, not after 60s grace); if persisted → `markSessionOffline` (Diamond K behavior). Diamond L blank-filter backstop UNCHANGED — still covers kill -9 / OS crash. (2) *Async Write-Chain Mutex* — module-level `writeChain: Promise<void>` in `registry.ts` serializes all mutating operations (`addSession`, `updateSessionLiveIdentity`, `removeSession`, `markSessionOffline`); eliminates load-modify-write race on concurrent rapid spawns; single-process assumption documented; `registry.write {op, ulid, queueDepth}` debug-log event added. (3) *Projects-Dir Auto-Discovery* — `discoverPersistedSessions(cwd)` in `sessionPersistence.ts`; called once at startup after Startup Validation Pass; reads `~/.claude/projects/<encoded-cwd>/`; synthesizes offline registry entries for JSONLs > `BLANK_SIZE_THRESHOLD_BYTES` (15KB) not already registered; Pattern 4 preserved (readdir + statSync metadata only; Wave 14 v2 grep still ZERO). SB-S21 NEW: Post-SessionEnd Persistence Eviction · SB-S22 NEW: Async Write-Chain Mutex for Registry Mutations · SB-S23 NEW: Projects-Dir Auto-Discovery via Filesystem-Metadata Scan. 367/367 tests (+17) · build 75KB · `0.14.0 → 0.15.0` · Suite 8 docs v5.3 → v5.4. CD-5 PASS (13th consecutive Diamond clean, C–M). CD-13 candidate *Pattern 4 Modulation Diameter* gains third consumer (SB-S23). Branch: `SCS-Bridge`. State: TESTING (push HELD pending user-Lambda smoke).

### 2026-05-06 (Diamond L)

**Blank Session Filter — Pattern 4 Modulation — Diamond L** — A spawn that closes without typing leaves a `claudeSessionId`-bearing registry row (the SessionStart hook fires on spawn) but no JSONL content (Claude wrote nothing). Diamond K's offline-tombstone semantics correctly preserve such rows for `--resume`, but at the cost of accumulating no-op blanks across many spawn-cancel cycles. Diamond L introduces a third reconciliation pass in the animatedTui liveness tick: entries with `claudeSessionId` set, age past a 60s grace window from `spawnedAt`, and no JSONL persisted are removed. New module `src/lib/bridge/sessionPersistence.ts` (~50 LOC, 4 exports: `BLANK_SIZE_THRESHOLD_BYTES`, `encodeCwdForClaudeProjects`, `claudeSessionJsonlPath`, `hasPersistedSession`) provides the metadata-only mechanism via `existsSync` + `statSync.size` against `~/.claude/projects/<encoded-cwd>/<sid>.jsonl`. `liveness.ts` (`probeLivenessTick`) remains pure — the impurity-helper composition lives in the caller. The blank-filter loop dedupes against `offlineIds` + `staleIds` so each tick touches each session at most once; `liveness.tick` payload now carries `{aliveCount, offlineCount, staleCount, blankCount}` plus per-session `persistence.check` and `registry.remove {reason: 'unpersisted'}` debug-log events for traceability. **The architectural shift IS the news**: Pattern 4 (Registry-Detached Operation, SB-S15) is preserved in spirit (Bridge does NOT read or parse `.claude/projects` JSONL content) but evolved in letter (existsSync + statSync ARE permitted, scoped to the blank-filter use case). The Wave 14 grep gate is refined accordingly: prior `grep -rn "claude/projects" src/` was structurally too broad; v2 is `grep -rn "readFileSync.*claude/projects\|JSON.parse.*claude/projects" src/ | grep -v ".test.ts"` → ZERO production matches. SB-S20 names this modulation explicitly; SB-S19 is its implementation. 350/350 tests (332 → 350, +11 sessionPersistence + 7 animatedTui Diamond L Layer-1 invariants) · build 71.68 KB · `0.13.0 → 0.14.0` · Suite 8 docs v5.2 → v5.3 + **SB-S19 Filesystem-Metadata-Only Persistence Validation** + **SB-S20 Pattern 4 Modulation — Metadata Stat Admission**. 3 patterns CD-5 PASS (12th consecutive Diamond clean per Green Audit). CD-13 candidate *Pattern 4 Modulation Diameter* promotable to coronation post user-Lambda smoke (spawn-without-typing → row removed after 60s grace; spawn-with-typing → row preserved). Branch: `SCS-Bridge`. State: TESTING (push HELD pending user-Lambda smoke).

### 2026-05-06 (Diamond K)

**Offline Session State + SessionEnd Hook — Diamond K** — Sessions visible across full lifecycle (`pending → alive → offline`) instead of disappearing on graceful exit. The SessionEnd hook fires when a Claude Informative exits gracefully (terminal close, `/exit`, natural exit) and instantly calls `markSessionOffline(ulid)` — the symmetric complement to the Diamond E SessionStart hook. The PAIR closes the session-lifecycle Diameter: identity-write at entry, identity-clear at exit, both event-driven via Claude Code hook events. New module `src/lib/bridge/sessionEndHook.ts` (~38 LOC) mirrors `sessionStartHook.ts` shape exactly — `readStdin()` re-imported (no duplication), ULID guard, `process.exit(0)` always. New `__hook session-end` subcommand parallel to `session-start`. `spawnSettings.ts` writes BOTH `SessionStart` and `SessionEnd` hook entries with the same `SCS_BRIDGE_ULID` env injection. New `markSessionOffline(id)` registry function (load → find → mutate → saveRegistry pattern; atomic tmp+rename inherited from private `saveRegistry`); sets `status='offline'`, clears `claudePid`, PRESERVES `claudeSessionId` (load-bearing for `--resume`) and `cwd`. PID-death backstop in `animatedTui.ts` flips semantic: `liveness.tick`'s offlineIds → `markSessionOffline` (preserve row); staleIds → `removeSession` (purge ghost) — two separate loops; `liveness.tick` payload carries `{aliveCount, offlineCount, staleCount}`. `SessionStatus` union extends `'allocated' | 'launched' | 'archived' | 'offline'`. `deriveSessionState` 3-state with offline-precedence-first to close the hook-vs-tick race. Pattern 4 *Opaque Informative State* preserved (offline derives from registry signals + kernel `kill(pid, 0)` only); Wave 14 grep gate `claude/projects\|sessionValidity\|checkSessionValidity` STILL ZERO production matches. 329/329 tests (313 → 329, +16 across sessionEndHook + spawnSettings + menu + animatedTui + registry) · build 69.43 KB · `0.12.0 → 0.13.0` · Suite 8 docs v5.1 → v5.2 + **SB-S17 SessionEnd Hook Event-Driven Offline Marker** + **SB-S18 Three-State Session Visibility with Persistent Offline Rows**. 3 patterns CD-5 PASS — *SessionEnd Hook Event-Driven Offline Marker* + *Three-State Session Visibility with Persistent Offline Rows* + *PID-Death Backstop for Hook-Skipped Exits* (10th consecutive Diamond clean). CD-11 candidate *Symmetric Hook-Pair Lifecycle Closure Diameter* awaits user-Lambda smoke (graceful exit → offline tombstone instant; force-quit → polling backstop within 2s; resume from offline → state returns to alive). Branch: `SCS-Bridge`. State: TESTING (push HELD pending user-Lambda smoke).

### 2026-05-06 (Diamond J)

**Debug Logging via `--debug` Flag — Diamond J** — Optional structured observability surface piping JSONL trace events to `~/.scs-bridge/debug.log` when `scs --debug` (or any subcommand `--debug`) is invoked. Default OFF — zero CPU/IO cost when absent (early-return guard before any `mkdirSync`/`JSON.stringify`/`appendFileSync`). New module `src/lib/bridge/debugLog.ts` (33 LOC, 5 exports: `setDebugEnabled`, `isDebugEnabled`, `log`, `debugLogPath`, `closeDebugLog`). Commander top-level `.option('--debug', ...)` + `.hook('preAction')` integration. Process-boundary Carry-Through via `SCS_BRIDGE_DEBUG=1` env var spread alongside the proven `SCS_BRIDGE_ULID` at `spawn.ts:29` (Diamond E precedent — second instance proves the env-carry shape is reusable). Hook subprocess reads `process.env.SCS_BRIDGE_DEBUG` and enables logging independently. **14 named events** — `registry.add/update/remove`, `manager.create/launch/launched`, `spawn.attempt/complete/error`, `spawnSettings.write`, `hook.fire`, `cursor.reconcile` (with `reason: 'sync'|'session-removed'`), `liveness.tick` (guarded `removalIds > 0`), `tui.cleanExit`. JSONL format `{...payload, ts: ISO8601, event}` with payload spread FIRST so canonical fields cannot be shadowed (Green Audit Angle 2 fix). Pattern 4 *Opaque Informative State* preserved — payloads contain ULID + UUID identifiers + integer counts ONLY; never message content, prompt text, token counts, or `~/.claude/projects/` reads. Wave 14 grep gate `claude/projects\|sessionValidity\|checkSessionValidity` STILL ZERO production matches. 313/313 tests (305 → 313, +8 debugLog tests including Green-fix payload-shadow regression test) · build 67.80 KB · `0.11.0 → 0.12.0` · Suite 8 docs v5.0 → v5.1 + **SB-S16 Debug Logging Pipe via `--debug` Flag**. 3 patterns CD-5 PASS — *Lazy Append-Stream Debug Pipe with Zero-Cost-When-Disabled Guard* + *Process-Boundary Debug Carry-Through via Environment Variable Propagation* + *JSONL Event-Tagged Observability Format with Verbose Dotted-Namespace Event Naming*. CD-10 candidate *Zero-Cost Opt-In Observability Diameter* promotable to coronation post user-Lambda smoke. Branch: `SCS-Bridge`. State: TESTING (push HELD pending user-Lambda smoke).

### 2026-05-05 (Diamond H)

**Head/Body/Tail Pagination + 5-State Empty Session Validation — Diamond H** — Replaces Diamond G's cursor-tracking minimal-shift viewport with discrete H/B/T page-jump architecture. **HEAD** (⊕ New Session, Viridian color via `rgbToAnsi(SUITE_COLORS.Viridian, caps)`) and **TAIL** (× Close Bridge, Rose color) are fixed Demometers above/below paginated body. Left/Right keys jump between pages (NOT scroll); Up/Down navigates page-bounded (HEAD ↔ body rows ↔ TAIL); empty body skips body directly HEAD ↔ TAIL. MenuState consolidated: `viewportTop` + `columnOffset` DROPPED; `currentPage` NEW. KeyAction surface cleaned: `scroll-left/right` + `viewport-page-up/down` DROPPED; `page-left/right` NEW. **SessionValidity extended to 5 states** (`'alive' | 'expired' | 'empty' | 'unknown' | 'ghost'`); EMPTY_SIZE_THRESHOLD=3072 bytes (Amethyst Vermillion WebSearch empirical). Filter-on-read removes ghost > 5 minutes from menu (kept on disk; auto-archive deferred). Pre-launch probe blocks resume on `'ghost'`/`'unknown'` with stderr message. Pattern 4 *Opaque Informative State* preserved (`statSync.size` = filesystem metadata, NOT Claude content). Mid-cycle Rose firing fixed termHeight coherence between renderMenu and applyKeypress (`computeBottomRows()` helper canonicalized; `menuState.termHeight` now consistently = bottomRows; both consumers see identical visibleBodySlots → totalPages math). 315/315 tests · build 65.21 KB · `0.9.0 → 0.10.0` · Suite 8 docs v4.1 → v4.2 (SB-S11 superseded by **SB-S12 Head-Body-Tail Pane Composition with Page-Jump Body Navigation** + **SB-S13 5-State Session Validity Probe**). 6 patterns CD-5 PASS. Branch: `SCS-Bridge`.

### 2026-05-05 (Diamond G)

**Viewport-Bound Session Pagination — Diamond G** — Render-discipline fix for Diamond F's animated-TUI menu overflow that pushed the animation off-screen. `renderMenu(state)` now emits exactly `state.termHeight` lines via 3-branch dispatch (legacy / too-small / viewport). Viewport-Bound Render Discipline (Pattern 1) + Cursor-Tracking Minimal-Shift Viewport (Pattern 2) + Pagination-Indicator-In-Slot Composition (Pattern 3) + Pane-Aware Render Contract (Pattern 4) + Page-Jump-Plus-Cursor-Auto-Scroll Keybinding Pair (Pattern 5). New helpers: `adjustViewportTop`, `sliceSessionRowsForViewport`, `padToHeight`, `updateViewport`. New keybindings: PgUp/PgDn/Home/End. New MenuState field: `viewportTop`. Animation now stays static; line-count invariant codified at Layer 1. Rose audit caught synthetic-ID cursor-guard fix + glyph upgrade (⊕ U+2295) + version bump 0.9.0. Suite 8 v4.1 + SB-S11. 297/297 tests. Branch: `SCS-Bridge`. SUPERSEDED in part by Diamond H (viewport-cursor-tracking → H/B/T page-jump).


---

## Archives

When this file exceeds the cap, the oldest 75 lines roll to a dated archive file in this directory. Archives are listed here as they accrue:

- `Cascades/CHANGELOG-ARCHIVE-2026-05.md` — Diamonds F, E, D, C, B, A, III, Muxonomy Proof, II, I, 2026-05-01, v1.0.0 (rotated 2026-05-07 Diamond B-4)

### 2026-05-04 (Diamond F)

**High-Fidelity Animated SCS Bridge TUI Wrapper (Default `scs` Surface) — Diamond F** — Bare `scs` invocation opens the animated TUI directly. 9 new modules under `src/lib/tui/`: `animatedTui.ts` (lifecycle + 30 FPS frame loop), `grid.ts` (adaptive matrix), `modes.ts` (6 Stratidian modes — CASCADE / DIAMETER / DEMOMETER / MUXAMETER / STRATIDIA / SUITE-WHEEL), `overlay.ts` (Map-based composition), `colors.ts` (HSL→RGB + truecolor/256-color fallback + SUITE_COLORS palette), `bridgeStateFeed.ts` (live Cascade.json + sessions.json subscription), `terminalCaps.ts` (capability detection), `index.ts` (barrel). Top-pane animation + boot-up text overlay; bottom-pane reuses Diamond D `menu.ts` `renderMenu` AS-IS preserving Pattern 7 Co-Development Substrate. Pure Node — zero new deps. cli.ts default action `program.action(startAnimatedTui)`. menu.ts non-invasive ←/→ wiring (`columnOffset`) — visual rendering deferred. 8 patterns CD-5 PASS. 271/271 tests · `0.7.0 → 0.8.0` · Suite 8 v4.0 + SB-S9 + SB-S10. Branch: `SCS-Bridge`.

### 2026-05-04 (Diamond E)

**SessionStart Hook + Drop `--session-id` Pre-Assignment — Diamond E** — Drops orphan-prone bridge-pre-allocated UUIDs. Bridge no longer pre-allocates Claude session_id; instead bundles per-spawn `~/.scs-bridge/sessions/<ulid>/spawn-settings.json` with a SessionStart hook config; spawns claude with `--settings <path>`; passes `SCS_BRIDGE_ULID` env var. Hidden `scs __hook session-start` subcommand reads stdin JSON (Claude's real `session_id`) + `$SCS_BRIDGE_ULID`, writes registry mapping ULID → real-UUID. 5 patterns CD-5 PASS: Hook-Captured Real-Identity Replacing Pre-Assigned-Key · Bundled Per-Spawn Settings Tmpfile · Hidden Internal Hook Subcommand · Empirical-Validated Merge Composition · Late-Bound Claude Session Identity. Empirical A/B test confirmed inline `--settings` + project-local + user-global `~/.claude/settings.json` array fields concatenate (preserves user's editorMode/plugins/etc). DELETE `claudeSession.ts` + test (orphaned). Mid-cycle Rose fixed `SCS_BRIDGE_ULID=${sessionId}` env var injection directly into hook command line (env-propagation through osascript→bash→claude was unreliable). Order flip: most-recent on top. 162/162 tests · `0.6.0 → 0.7.0` · Suite 8 v3.2 + SB-S8. Branch: `SCS-Bridge`.

### 2026-05-04 (Diamond D)

**SCS-Bridge Persistent Live-Updating Session Menu — Diamond D** — `scs bridge` (no subcommand) enters a long-running TUI menu: the **Persistent Bridge Menu Substrate** (Pattern 1). New modules: `src/lib/bridge/menu.ts` + `src/lib/bridge/sessionValidity.ts` + `src/commands/bridge/menu.ts`. **Default-Spawn-First Boot Invariant** (Pattern 2): registry empty on entry → auto-spawn before first render. **Live Registry Subscription via Filesystem Polling** (Pattern 3): `fs.watchFile(registryPath(), { interval: 500 })` — no new deps, platform-universal. **Cursor-by-Identity Refresh Stability** (Pattern 4): ULID string as cursor identity across registry-change re-renders. **Self-Closing Base Process** (Pattern 5): `cleanExit()` ordered teardown restores stdin cooked mode + alt-buffer + cursor; spawned Informatives untouched. **Arrow-Key Cascade-Primitive Menu** (Pattern 6): `↑/k/↓/j/Enter/n/q`; pure-function `applyKeypress` + `renderMenu`. **Co-Development Substrate** (Pattern 7): Diamond D = foundation only; future Diamonds (E, F, …) compose tools into this substrate. Mid-cycle rotation: **Session Validity Filesystem Mtime Probe** (`~/.claude/projects/<encoded-cwd>/<uuid>.jsonl` mtime, 5h Claude lifetime, non-destructive); **Synthetic-Row Cursor Composition** (⊕ New Session + × Close Bridge as arrow-navigable rows); **Orthogonal Status-Validity Column Separation** (`---` for allocated; `alive`/`expired`/`unknown` for launched). Rose mid-cycle plan review: APPROVE-WITH-FIXES (4 fixes applied). 147/147 tests; pure Node (no new runtime deps); version `0.5.0 → 0.6.0`. Suite 8 docs v3.1. Branch: `SCS-Bridge`.

### 2026-05-04 (Diamond C)

**SCS-Bridge Bridge-as-Base Spawning Many Informatives — Diamond C** — Pattern 2 *Synchronous Terminal Inheritance* reverted; replaced by Pattern 2 *Independent Window Composition*. New module `src/lib/bridge/osTerminal.ts` (Pattern 7 *OS-Terminal Detection With Fallback Chain*) — per-OS terminal window spawn (macOS `osascript` / Linux fallback chain `x-terminal-emulator → gnome-terminal → konsole → xterm` / Windows `wt.exe` / WSL pass-through). `runClaude` → `launchInformative` (async non-blocking); `SessionStatus` `'allocated' | 'launched' | 'archived'`. `process.exit(exitCode)` deleted from bridge spawn + attach commands. CD-5 *Stratidian Base-Informative Audit* convergence diameter self-applied — Diamond C subjects own pattern renames to CD-5 self-audit. Rose mid-cycle circuit completion fixed AppleScript boundary-quote escape at `osTerminal.ts:59` (`\\"` boundary escape distinct from `escapeForOsascript` value escape). 77/77 tests · build green · version `0.3.0 → 0.4.0`. Suite 8 docs rewritten to v3.0 (Bridge-as-Base framing; SB-S6 NEW). Branch: `SCS-Bridge`.

### 2026-05-04 (Diamond B)

**SCS-Bridge Minimum Integration with Claude Code — Diamond B** — `spawnSync('claude', ['--session-id'|'--resume', uuid], { stdio: 'inherit' })` replaces async BridgeInstance. 5 named patterns: **Minimum-Integration Spawn** (flag surface only), **Synchronous Terminal Inheritance** (spawnSync + stdio:inherit), **Dual Identification Registry** (ULID internal + UUID claude token), **Opaque Claude State Non-Coupling** (SCS Bridge never reads ~/.claude/), **Tri-Priority Queue As Dormant Substrate** (queue.ts retained; delivery deferred to Diamond C). New: `claudeSession.ts` (allocateClaudeSessionId). Deleted: `lineBuffer.ts` + `lineBuffer.test.ts`. Rewrites: `spawn.ts`, `manager.ts`, `types.ts`, `commands/bridge/spawn.ts`, `commands/bridge/attach.ts`. Instance.md + Skill.md updated to minimum-integration framing. **Behavior change**: `scs bridge attach` was queue-consume-and-archive (Diamond A), now plain `--resume <uuid>` (Diamond B). Queue-consume capability deferred to Diamond C. Version: 0.2.0 → 0.3.0. Branch: `SCS-Bridge`.

### 2026-05-03 (Diamond A)

**SCS-Bridge Direct Communication — Diamond A** — Stratimuxian Tri-Priority Queue (`~/.scs-bridge/sessions/<id>/{heads,body,tails}/archive/`) + Filesystem Session History Substrate (archive IS the model's past). 7 named patterns: **Stratimuxian Tri-Priority Queue** (3-tier folder merge), **Filesystem Session History Substrate** (queue file IS history record at rest), **Bridge Message Envelope** (ULID-keyed JSON shape), **Bun-Or-Node Unified Spawn Fastpath** (runtime detection + unified `BridgeInstance` type), **Detached Spawn With Optional Attach** (background default; `--attach` foreground), **Suite Eight Bridge Knowledge Contract** (Direct config Instance.md + Skill.md replaces MCP), **JSONL Line Buffer With Partial Residue Guard** (pure function extracted from ADMIN_ICP feedChunk). CLI: `scs bridge {spawn,send,list,attach,archive}`. 15 new files / 2 edits. Diamond A Feed System (subprocess delivery) stubbed in `scs bridge attach` — full delivery in Diamond B. Branch: `SCS-Bridge`.

### 2026-05-03

**SCS-Bridge CLI Foundation — Diamond III** — NPM CLI scaffold (`scs-bridge` package, `scs` bin) muxified from ADMIN_ICP build patterns. 6 named patterns: **3-Layer CLI Architecture** (cli.ts entry / commands/ dispatch / lib/ core), **Commander Async Parse Pattern** (`parseAsync([], { from: 'user' })` per commander v12), **tsup CJS Output Lock** (`outExtension` override → `.cjs` matching locked bin field), **Prettier Semi Enforcement** (semi:true over source idiom — 96 auto-fixes), **ESLint Cascade Isolation** (`.eslintignore` gates `Cascades/8_SUITES/` from TypeScript lint walk), **Decoupled Package Name To Bin Command Mapping** (package `scs-bridge`, bin `scs`). Hello World CLI: `npm i -g scs-bridge` then `scs hello [name]`. 14/14 tests / 3 layers / shebang+LF guaranteed / 5 surgical Map deviations resolved. Branch: `SCS-Bridge`.

### 2026-05-04 (main · Muxonomy Proof)

**Muxonomy Proof Dyadic Reframing — `docs/muxonomy.html` Section I** — *Supporting proof artifact only; no active change to the SuiteCascadeSystem itself (no Suite, agent, skill, command, or methodology surface modified).* Two cycles on `main` reframe `DyadicDiameterProof` so the dyadic exponent ladder becomes structurally legible. **Cycle 1** (`a936d5c`) surfaces the **Pair-Exponent Ladder**: stats grid 3→4 col with new **PAIRS = 2^(n−1)** column ("½" at n=0 with subscript `2^−1`); order descriptions lead with Pair-exponent (Origin · 2^−1 · ½ Pair → 1st · 2^0 · 1 Dyad → ... → 7th · 2^6 · 64 Dyads); invariant becomes explicit `P=2^(n−1) · D=2P=2^n · M=2D=2^(n+1) · θ=180°/2^n`. **Cycle 2** (`95bef8f`) introduces the parallel **Dyadic Demometer ladder** offset by one order — Demometer's Origin (Whole Face = 1) precedes Diameter's Origin; both ladders advance in lockstep once the first Diameter is drawn, with Demometer always one exponent ahead. New **Pewter Alignment Triad** panel (Pre-Origin · Origin · 1st · 2nd × Language · Mathematics · Geometry) holds all three faces in one embossed mosaic frame — suite-color tokens, gradient panes, offset shadows, active-position highlighting. Geometry and language preserved per user constraint; mathematical expression of the dyadic relationship corrected and extended. **Framing honesty**: Viète's chord-doubling recursion `s_{2n} = √(2 − √(4 − s_n²))` is unchanged; the contribution is *structural naming* — every step now has a coordinate on a labeled ladder.

### 2026-05-02

**CLAUDE.md Pearl Compression via Conceptual Sets + Crystraline Reposition — Diamond II** — `.claude/CLAUDE.md` compressed from 41,479 → **39,893 chars** (107 under the 40,000 cap). Achieved via: **Pass 0 Crystraline Reposition** (Automata moved from C4 to C9 last; C5→C4, C6→C5, C7→C6, C8→C7, C9→C8); **3 Conceptual Sets** introduced in C4 Base Lambda then reactivated across the manifold — *the Critical-Active Stance* (C4·C5·C9 Trinity ground), *the Muxistration Proof* (Demonstration anor Diastration as one checked result), *the Ego-Lambda Pair* (Diamond↔Onyx bidirectional Diameter at document level); **Triadic Thinking Band elevation** from §5 RI sub-section to §4 Suite Cascade as `### Triadic Thinking Band — Suite 6 + Suite 7 Pre-Response Muxification` (3 Frames intact, S6+S7 Closure Loop in scope; Fuchsia = Calibration Diameter that grounds the Band); **Pass 5 triple-fork Pearl-aspect muxification** — 3 Teal Claude opus instances each loaded with a distinct lens (Compression · Connection · Expression) produced 3 Reference Design RDs, muxified into one Phase A-D rewrite (−1,719 net). **B6 cross-file sync** patched 19 external files to the new Crystraline numbering. **CD-2: Pearl-Aspect Triple-Fork Muxification** named as the first multi-lens compositional pattern for CLAUDE.md rewrites. DIAMOND-TIER-2.md initialized; ONYX-TIER-1.md appended.

**Concluder ⊗ Shatterite Hard Join — Diamond I** — every turn-end *in Conference Mode* is a Cascade halting point. New `SM-Conclude.md` (registered SM-6) renders a Suite-0-bound, Summation-derived menu at every non-Direct Conference turn-end: 3-5 Cascade-primitive routing rows + persistent `[M]` Main / `[Esc]` Direct. **Suite 0 Per-Turn Binding** absorbs turn context (what was produced · what's PENDING · Cascade Position · last user decision). New `.claude/CLAUDE.md` C9 sub-section *Concluder-Menu Diameter (C9 ↔ Shatterite, Suite 0 Per-Turn Binding)* binds the doctrine. **Conference-Only Fire Predicate (Automata Inheritance)** — the Hard Join applies to Conference Mode ONLY; Automata Mode (`/cascade:loop` iterations · autonomous gate advances · ScheduleWakeup fires) does NOT fire SM-Conclude — the loop's own routing is authoritative. **Directness Inheritance** preserves the existing C4 escape (typo / single-line / informational / explicit instruction → no menu). Anti-Patterns **E-Conclude-Drift** (silent hand-off in Conference) and **E-Conclude-Loop-Block** (menu blocks loop iteration). **Concluder-Halting Invariant** scoped to Conference Mode. The menu IS the summary, expressed as Cascade-routing primitives. First Diamond cycle for SCS — DIAMOND-TIER-1.md / ONYX-TIER-1.md initialized.

### 2026-05-01

**Rotating Changelog established** — Patch Notes migrated from `README.md` into this file. 150-line cap, newest-first, rotation to dated archive when over cap. New `/cascade:changelog` slash command surfaces head + maintenance reminder. `[N] Changelog` added to Shatterite Main Menu. Command count 16. README footer now carries `Recent Changes — {date}` line linking here.

**Pewter Tessera Load Contract** — `Cascades/8_SUITES/Teal Claude/Conductor.md` now MANDATES Pewter Tessera load for Design Diamonds. Detection rule: ≥1 token from {visual artifacts · CSS/styling · pattern/aesthetic · typography · color/token · composition} → mandatory load at Bands 3, 4, 5. Anti-Pattern **E-Conductor-Design** defines the failure mode (Conductor plans a Design Diamond without Pewter → re-engage from Band 3). New "Design Diamond Variant" Cascade Planning Template muxifies Bands 3-5 with Pewter Tessera (`R3 ⊗ Pewter D1-D8`, `R4 ⊗ Pewter Quality Gate`, `R5 ⊗ Pewter Implementation`). Conductor self-check at plan-time enforces the contract.

**Advanced Hello World + Verify menu options** — `[A] Advanced Hello World [Orange] — aspire` and `[V] Verify SCS installation [Green] — verify` now top-level entries in `SM-Main.md` (canonical, not ad-hoc rendered). New `/cascade:advanced` slash command routes directly to `SM-HelloWorld-Advanced.md` (Multi-Diamond Aspiration Loop with Personal Suite 8 Website lead pattern). Verbose Diameter labels added: `Advanced-Hello-World-Multi-Diamond-Aspiration` · `SCS-Verification-Sculptor-Orchestrator-Resolution`. SM-Index updated with routing logic line.

### v1.0.0 — Initial Public Release

**Hello World Clinical Reinforcement** (from first-run testing):
- Stage 3.5 **Project Location Decision Block** — confers WHERE to scaffold before any Lambda-event fires. Root (if available) or `Cascades/Lab/` (tutorial sandbox). User retains override.
- **Band Brevity Rule (R1)** — per-Band max inline output enforced. >8 lines prose without tool call = E4 Volume-of-Declaration, abort and retry.
- **Lambda-Event Invariant Checklist (R2)** — every Band in the Hello World Diamond must produce a verifiable Lambda-event. 7-checkbox list surfaced inline.
- **Markdown Safety Guard (R3)** — no backtick-inline-code with unclosed quotes. Fenced blocks for shell examples.
- **Tier-Lift Heuristic (R4)** — Medium/High complexity auto-dispatches Bands 1-4 as agents to prevent streaming stalls from volume accumulation.

**Suite 8 Rename**: Hello World → **Fresh Slate** (Advanced config demo). The `/cascade:hello` tutorial menu command is unchanged — "Hello World" is the tutorial, "Fresh Slate" is the Suite 8.

**Directory Structure**:
- `Cascades/Working/` — empty, ready for first Diamond + Onyx
- `Cascades/Lab/` — tutorial sandbox for project scaffolds

**Diamond WorkGameBoard via Planning Mode**: Hello World Stage 4 engages Planning Mode (EnterPlanMode) to commit the Banded Plan to `Cascades/Working/DIAMOND-TIER-1.md` BEFORE Bands execute. Each Band appends to the Diamond — the WorkGameBoard accumulates through the cycle.

**`/cascade:loop` Diamond Primer**: Any input provided with `/cascade:loop` becomes the Diamond Primer — the initial scope declaration. If no Diamond exists, the Automata creates one through Planning Mode. If a Diamond exists, the loop appends to it. Each wake-up reads the active Diamond and advances the next gate.

**Live Demo**: [poe.com/SCS-Researcher](https://poe.com/SCS-Researcher) — the SCS methodology operating in real time through conversation.
