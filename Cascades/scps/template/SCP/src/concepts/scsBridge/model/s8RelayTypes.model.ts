/**
 * s8RelayTypes.model.ts — the REWRITE-PROOF Suite 8 WIRE TYPES (C760 · the BO-1 law, third seat)
 *
 * THE CLASS THIS CURES (the Frontier Beginnings determination): the server's central N-watcher
 * broadcasts menu-stage relays under the SUITE8-typed action strings for EVERY designation —
 * but the Forge's token rewrite renamed the actionExchange keys in every generated concept's
 * muxonomy ('Suite8 Set Designation Menu Stage' → 'FrontierBeginnings Set ...'), so the
 * incoming stream matched NOTHING in a generated page's muxium and silently dropped. The
 * floors masked it; the live edit exposed it.
 *
 * THE LAW (s8Anchor · s8Routes · now s8RelayTypes): wire contracts live in a NEVER-COPIED
 * scsBridge model whose exported names carry NO `suite8` token, so the mint rewrite cannot
 * touch them. A generated concept's exchange then routes the SERVER'S dialect into its OWN
 * local quality (qualityName stays rewritten — that is correct), and the keyed Record fills.
 *
 * The literals here MUST byte-match the Huirth-side relay creators (TQNI):
 *   suite8SetMenuStage.quality.client · suite8SetDesignationMenuStage.quality.client.
 */

/** The scalar single-designation menu-stage relay (the GTMS8C legacy watch). */
export const S8_MENU_STAGE_RELAY_TYPE = 'Suite8 Set Menu Stage';

/** The keyed per-designation menu-stage relay (the N-watcher SMRP broadcast). */
export const S8_DESIGNATION_MENU_STAGE_RELAY_TYPE = 'Suite8 Set Designation Menu Stage';
