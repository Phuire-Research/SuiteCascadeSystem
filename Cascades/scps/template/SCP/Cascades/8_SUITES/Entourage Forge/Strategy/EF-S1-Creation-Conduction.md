# EF-S1 — the Creation Conduction (the Banded Vermillion, executable)

<VermillionPlan topic="Suite 8 Creation — {mintedName}">

Band 0 (the Forge · Absorb):
  Informative: the minted scaffold read — Instance.md (Domain TBD) + Maintainer.md.
  Actionable: confirm the mint; open Band 1.

Band 1 [F1 Domain Elicitation] (Conference):
  Informative: the user's answer to "What Domain shall {mintedName} tend?"
  Actionable: the Domain written into the minted Instance.md; the query scope drafted.

Band 2 [F2 Planned Query — Cadmium Researcher] (Comprehensive):
  Informative: PER STAGE, declared: the user's SYSTEM (files · configs · running state)
    anor WebSearch (the domain's external ground) — the domain decides each stage's source.
  Actionable: Reference Designs written under the minted Suite 8's dir.
  Suite 8: Cadmium Researcher — the branch-tagged step form; citation-grade.

Band 3 [F4 Architecture Ground] (the Forge · runs WITHIN Band 4):
  Informative: the direct read — the SCS Bridge Instance anor the bridge-root
    Researcher Instance, into context, current reality only.
  Actionable: the ground held open while Band 4 strikes.

Band 3.5 [Page Duplication — the compiled registration] (the Forge · script invocation):
  Informative: the minted name (PascalCase + display form); the branch MUST be b/ (the
    door's Turn Over Request put the conduction here — verify before invoking).
  Actionable: `cd {workspace root} && scs suite8:page {NamePascal} --display-name "{minted name}" --designation {thisScp}` (+ `npm run build:client` on the SCP — the gate chain ends at tsc)
    (NO --home) — the proven S9-DomainPageCreate pipeline: copy · rename · the three
    anchored registration inserts · tsc · build:client. Concluders: the command's
    created signal · tsc 0 · build green.

Band 4 [F3 Concept Enhancement] (Analytical):
  Informative: the Reference Designs + the held ground.
  Actionable: the minted Instance.md struck upward (Position table · Etymology ·
    domain-named Skill Signatures) + Skill files where the domain demands.

Band 5 (the Forge · Verify):
  Informative: the enhanced Concept read back against the RDs.
  Actionable: mismatches struck again; the Concluders run (wc · grep the signatures).

Band 6 [F5 the Close] (Conference):
  Informative: the user's sight of the final in-context hand-off menu (AskUserQuestion —
    the conduction's summary as the prompt) + the STARTING MENU section written into the
    minted Onboard.md.
  Actionable: the minted Onboard.md REWRITTEN (creation body out · domain body +
    Starting Menu in) · THE CLEANUP CONCLUDER (`grep -c "Domain: TBD"
    Cascades/8_SUITES/{mintedName}/Instance.md` = 0 — the pane's dissolution proven) ·
    the hand-off declared with the note: the Forge stays spawnable via the SCS-Bridge.

</VermillionPlan>
