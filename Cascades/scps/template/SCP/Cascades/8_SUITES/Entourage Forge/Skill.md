# Entourage Forge — Skill Document

The five signatures (F1-F5) declared in Instance.md, operationalized. The Forge is a
Conductor: every signature names WHO executes (the Forge in-context anor a dispatched
Suite 8) and WHAT artifact proves it (the Muxistration — narrative alone is nothing).

## F1 · Domain Elicitation
**Executor**: the Forge Anchor (its own Onboard.md rides the spawn as the seed prompt).
**Mechanic**: an in-context Shatterite Menu — the Forge fires `AskUserQuestion` with
the domain question, candidate domains derived from the minted name as options, and the
user's free text (the tool's Other) first-class. The name ALREADY exists (the mint
precedes the Forge); the domain is the missing half of the Demometric Concept. THE
SURFACE LAW: the Forge's terminal IS the Conference surface — no `Cascades/Extended/`
menu files, no relay wiring to verify; the tool renders directly to the user.
**Artifact**: the domain recorded into the minted Suite 8's Instance.md `Domain:` line
(replacing the scaffold's TBD).

## F2 · Planned Query Conduction
**Executor**: Cadmium Researcher (dispatched — the Band 1 hand-off).
**Mechanic**: the Forge formalizes a Planned Query in the branch-tagged step form —
each stage declares its Informative source (the user's SYSTEM: files, configs, running
state — anor WebSearch for the domain's external ground) and its Actionable product.
**Artifact**: Reference Designs written under the minted Suite 8's dir (the research
that becomes the Concept's substance).

## F3 · Concept Enhancement
**Executor**: the Forge (in-context — the Teal Claude skill-creation inspiration).
**Mechanic**: from the Reference Designs, author the minted Concept upward: the
Instance.md gains its Position table, Etymology, and Skill Signatures named for the
domain; Skills gain files where the domain demands them. The scaffold's barebones is
the anvil — never discarded, always struck.
**Artifact**: the enhanced Instance.md + any Skill files, Read-back verified — struck
AFTER Band 3.5's Page Duplication (`scs suite8:page` — the compiled registration that
makes the Suite a REAL navigable page; see Conductor.md §Band 3.5), so the enhancement
lands on both the Concept documents anor the duplicated page's own surface.

## F4 · Architecture Grounding
**Executor**: the Forge (in-context — the direct-read idiom).
**Mechanic**: read the SCS Bridge Instance anor the bridge-root Researcher Instance
directly into context WHILE enhancing — the Suite 8 under creation binds to the
architecture as it IS, not as remembered.
**Artifact**: citations in the enhanced Concept that resolve against the read ground.

**F4 IS the Forge-directed Bridge-Architecture Interchange Load** (the SCP Researcher's
SCP-S21 · the Two-Muxonomies Load, performed under the Forge's conduction). This dual
in-context read directs two unlike Suite 8s as one bound act: the **SCP Researcher**
supplies protocol knowledge, the **SCS Bridge** supplies session means — held in co-focus
via the shared Pattern-4 invariant (`bridge never reads ~/.claude/`). The Forge grounds the
read against a commissioned, **gleanable** Reference Design — e.g.
`Cascades/Documentation/Cascades/RD-SHATTERITE-MENU-SESSION-INSTILLATION.md`, which
self-declares the Entourage Forge as its consumer — then Band 3.5 (`scs suite8:page`)
compiles the page that instantiates the very session-means circuit the RD grounds (the
Anchor-writes-`menu.json` ↔ watcher-relays-`MenuStage` ↔ page-renders-options ↔
Anchor-receives-dispatch loop). The Researcher-loads-Bridge protocol (for a question) and
this Forge-reads-both grounding (for a page) are the same inline-interchange organ seen from
two sides.

## F5 · The Actualization Close
**Executor**: the Forge (in-context).
**Mechanic**: TRUNCATE the spawning placeholder — edit the minted Suite 8's Onboard.md:
the creation-flow body leaves; the domain body arrives, closing with the STARTING MENU
section (the minted Suite's own future anchor greets its user the same way — in-context
`AskUserQuestion` Shatterite Menus, one decision per stage). Then the final in-context
hand-off menu: the conduction's summary as the prompt, visit-the-page anor adjust as
options.
**Artifact**: the rewritten Onboard.md (Starting Menu section present) + THE CLEANUP
CONCLUDER: `grep -c "Domain: TBD" .../Instance.md` = 0 — the Forge-launch pane's
dissolution proven before the close. The Forge remains spawnable via the SCS-Bridge
after the pane goes — the dissolution removes the door, never the Forge.
