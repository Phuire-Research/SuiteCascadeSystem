> **THE GEOGRAPHY RESOLUTION LAW (C868 · load-bearing · anchors AND workers)**
> Your RI home is ABSOLUTE: `<SCP_ROOT>/Cascades/Extended/Pewter Tessera/` where
> **SCP_ROOT = the Dock §4 stamp** in this composed prompt (fallback: `curl -s
> http://localhost:<scpPort>/scp-config` → `extendedRoot`). Your working directory is the
> WORKSPACE root — NOT the SCP package — so every relative `Cascades/Extended/…` anor
> `Cascades/scps/template/SCP/…` path in this document MUST be resolved against SCP_ROOT
> before any read anor write; installs RENAME the SCP dir, so a baked template path is the
> WRONG citizen. **Spawning any worker** (`scs_spawn_suite8_session`): ALWAYS pass
> `scpName` = your own citizen. If SCP_ROOT is unresolved in §4, HALT and say so.

# Pewter Tessera — Aspect Transplant Manifest (Maintainer.md)

## Preamble — the Boundary Law

Designation, Home SCP, and Dependencies are **identity-bearing** at any future update
seam. The part-renewal doctrine that guards the SCP self-update circuit (C293: identity-bearing
files take disposition WRITE — full theirs; NEVER patch) applies one level up to Suite-8 identity:
a partial update MUST NOT revert this Suite 8's Designation, re-home it, or silently drop a
declared Dependency. The Sovereignty Boundary below names what is portable and what is fixed —
the boundary makes transfer meaningful; this manifest makes the boundary crossable.

## Sovereignty Boundary

- **Home SCP**: template
- **Installed-in**: any SCP the template scaffolds (rides the install clone from
  `Cascades/scps/template/SCP/Cascades/8_SUITES/`)
- **Portable**: this entire Suite 8 dir (Instance.md · Skill.md · Skills/) travels with the install.
  The HiFi design tokens the user cascades via `hifiConfig.json` (factory < JSON < localStorage)
  are the per-SCP expansion surface.
- **Fixed**: Designation (`Pewter Tessera`) — byte-must-match the dir name for roster/anchor binding.

## Skills Registry

| Skill | File | Currency Gate (one-liner) | Last Executed |
|-------|------|---------------------------|---------------|
| AddSvgPattern | `Skills/AddSvgPattern.md` | The SVG-pattern authoring recipe still matches the current pane/border token surface | UNKNOWN |
| SetColorsViaJson | `Skills/SetColorsViaJson.md` | The factory < JSON < localStorage colour cascade still holds | UNKNOWN |
| S-STRATIPUNK | `Skills/S-STRATIPUNK/Skill.md` | The Stratipunk styling variant still describes a live HiFi aesthetic | UNKNOWN |

## Muxonomy Registration

- **Designation**: Pewter Tessera (Instance designation: Tessera)
- **Configuration**: Direct
- **Domain**: HiFi Design System — design tokens, SVG patterns, gradient panes, embossed borders,
  typography, button variants.
- **Dependencies**: the SCP's `hifiConfig.json` (per-SCP token override surface); the SCP's Vue
  HiFi component family (`.hifi-pane-*`, `.hifi-btn-*`). No cross-Suite-8 dependency.

## Cascade Position Marker

- **Gate**: UNKNOWN
- **PENDING**: none evident
